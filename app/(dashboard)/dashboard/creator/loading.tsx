import { DashboardSkeleton } from '@/components/dashboard/dashboard-skeleton';

/** Instant loading UI for every /dashboard/creator/* navigation. */
export default function CreatorDashboardLoading() {
  return <DashboardSkeleton />;
}
