import { DashboardSkeleton } from '@/components/dashboard/dashboard-skeleton';

/** Instant loading UI for every /dashboard/business/* navigation. */
export default function BusinessDashboardLoading() {
  return <DashboardSkeleton />;
}
