'use client';
/** Overview — the hub's home. Cross-source KPIs, comparison chart, top strips. */
import { Overview } from '@/components/analyticshub/overview';

export default function AnalyticsHubOverviewPage() {
  return <Overview />;
}
