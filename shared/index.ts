/**
 * Shared, framework-agnostic domain types and constants used by BOTH the
 * Express backend and the Expo mobile app. Import from here, e.g.:
 *
 *   import { Campaign, REWARD_TYPES, canTransitionCampaign } from '../shared';
 */
export * from './types';
export * from './constants';
