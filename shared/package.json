{
  "name": "@collably/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types + constants used by both the backend and the mobile app.",
  "//": "type:commonjs so the CommonJS backend (tsconfig module:node16) can import these sources and require the compiled dist/shared output at runtime. The mobile app (Metro/Babel) transpiles the .ts sources directly and is unaffected.",
  "type": "commonjs"
}
